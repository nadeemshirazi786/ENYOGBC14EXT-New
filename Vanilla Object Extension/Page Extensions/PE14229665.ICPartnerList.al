pageextension 14229259 "IC Partner List ELA" extends "IC Partner List"
{
    layout
    {
        addlast(Content)
        {
            field("Use for IC PO Lookup ELA"; "Use for IC PO Lookup ELA")
            {
                Caption = 'Use for IC PO Lookup';
                ApplicationArea = All;
            }

        }
    }
}